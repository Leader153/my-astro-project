---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import cardImg from "../assets/Romantic cruise 2.webp"; // Placeholder
---

<Layout
    title="הפלגה משפחתית"
    description="יום בים עם כל המשפחה — בטוח, מהנה ובלתי נשכח לכל הגילאים"
>
    <div class="service-page" dir="rtl">
        <div class="container">
            <h1>הפלגה משפחתית</h1>
            <p class="subtitle">
                יום בים עם כל המשפחה — בטוח, מהנה ובלתי נשכח לכל הגילאים
            </p>

            <div class="image-wrapper">
                <Image
                    src={cardImg}
                    alt="משפחה שמחה עם ילדים נהנים מהפלגה ביאכטה בים"
                    width={800}
                    height={500}
                    class="main-img"
                />
            </div>

            <div class="content">
                <p>זמן איכות משפחתי אמיתי בחיק הטבע, הרחק מהמסכים ומהעיר.</p>
                <p>
                    היאכטות שלנו בטוחות מאוד לכל גיל, ויאפשרו לכולם ליהנות מזמן
                    ביחד באווירה בלתי רגילה ומיוחדת.
                </p>

                <a
                    href="https://wa.me/972533403449"
                    target="_blank"
                    rel="noopener"
                    class="cta-btn">הזמינו עכשיו ב-WhatsApp</a
                >
            </div>
        </div>
    </div>
</Layout>

<style>
    .service-page {
        padding: 120px 20px 60px;
        background: #f8fbff;
        min-height: 100vh;
        font-family: "Rubik", sans-serif;
    }
    .container {
        max-width: 800px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 20px;
        padding: 3rem;
        box-shadow: 0 10px 40px rgba(8, 4, 126, 0.06);
        text-align: center;
        border-top: 5px solid #059669;
    }
    h1 {
        font-size: 2.5rem;
        color: #08047e;
        font-weight: 800;
        margin-bottom: 0.5rem;
    }
    .subtitle {
        font-size: 1.2rem;
        color: #0f7ab0;
        margin-bottom: 2rem;
        font-weight: 500;
    }
    .image-wrapper {
        margin-bottom: 2rem;
        border-radius: 15px;
        overflow: hidden;
    }
    .main-img {
        width: 100%;
        height: auto;
        display: block;
        aspect-ratio: 16/9;
        object-fit: cover;
    }
    .content p {
        font-size: 1.1rem;
        line-height: 1.8;
        color: #000000;
        margin-bottom: 1rem;
    }
    .cta-btn {
        display: inline-block;
        margin-top: 2rem;
        padding: 1rem 2.5rem;
        background: linear-gradient(135deg, #d4a017, #f0c040);
        color: #08047e;
        font-weight: 800;
        font-size: 1.2rem;
        border-radius: 12px;
        text-decoration: none;
        box-shadow: 0 8px 25px rgba(212, 160, 23, 0.4);
        transition:
            transform 0.2s,
            box-shadow 0.2s;
    }
    .cta-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 30px rgba(212, 160, 23, 0.6);
    }
</style>
