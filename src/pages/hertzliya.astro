---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import PremiumSwitch from "../components/PremiumSwitch.astro";
import cardImg from "../assets/Romantic cruise 2.webp";
import marinaImg from "../assets/marina-herzelia.webp";
import marunaImg from "../assets/maruna-herzelia.webp";

const services = [
  {
    icon: "⛵",
    title: "קטמרן עד 21 מפליגים",
    desc: "יאכטת קטמרן ענקית ומרווחת להפלגות קבוצתיות גדולות, ימי הולדת ומסיבות מרובות משתתפים",
    tag: "לקבוצות",
    color: "#059669",
    alt: "הפלגת קטמרן עד 21 איש במרינה הרצליה",
    href: "/קטמרן-עד-21-מפליגים",
  },
  {
    icon: "🥂",
    title: "שייט רומנטי",
    desc: "חוויה אינטימית ומושלמת לזוגות אוהבים בשקיעה, כולל יין ומוזיקה לבחירתכם",
    tag: "לזוגות",
    color: "#e91e8c",
    alt: "שייט רומנטי במרינה הרצליה",
    href: "/שייט-רומנטי",
  },
  {
    icon: "🍽️",
    title: "ארוחה רומנטית על יאכטה",
    desc: "ארוחה זוגית מפנקת על סיפון היאכטה עם נוף לים, נרות, שמפניה ואווירה שלא תשכחו",
    tag: "רומנטי",
    color: "#e91e8c",
    alt: "זוג נהנה מארוחה רומנטית על גשר יאכטה מול מרינה הרצליה",
    href: "/ארוחה-על-יאכטה",
  },
  {
    icon: "🎣",
    title: "דייג",
    desc: "יום דייג מהנה בים — ללא צורך ברישיון דייג. הכל כלול: ציוד, הדרכה וסקיפר מנוסה",
    tag: "חוויה",
    color: "#0f7ab0",
    alt: "דייג חוויתי מיאכטה במרינה הרצליה ללא רישיון",
    href: "/דייג",
  },
  {
    icon: "⛵",
    title: "הפלגת סקיפר ליום אחד",
    desc: "יום שלם בים עם סקיפר מקצועי — שיזוף, נוף, מוזיקה ויין. חוויה מהסרטים",
    tag: "פופולרי",
    color: "#7c3aed",
    alt: "הפלגה ביאכטה עם סקיפר מקצועי ליום אחד מהרצליה",
    href: "/הפלגת-סקיפר-ליום-אחד",
  },
  {
    icon: "🌙",
    title: "צימר על הים",
    desc: "לינה על יאכטה — חוויה יוקרתית וייחודית ממש בים. בקתה צפה עם כל הפינוקים",
    tag: "יוקרה",
    color: "#d4a017",
    alt: "לינת צימר על יאכטה פרטית בלילה מול מרינה הרצליה",
    href: "/צימר-על-הים",
  },
];

const textSections = [
  {
    icon: "👨‍✈️",
    title: "קבלת פנים אישית",
    text: "בבואכם למרינה הרצליה הסקיפר יקבל את פניכם בחיוך, ילווה אתכם ליאכטה, יגיש לכם ארוחות, שתייה, יקשט, יפנק, וישיט אתכם במקצועיות בטיחות והנאה רבה במטרה לצרוב בזיכרונכם חוויה קסומה וייחודית ולחשוף אתכם לעולם הנפלא של שייט בהרצליה ויאכטות.",
  },
  {
    icon: "⚓",
    title: "מיקום יוקרתי ופרטי",
    text: "היאכטות שלנו עוגנות מול טיילת המסעדות, במעלה הרציף אליו רק מורשים יכולים להיכנס. כך שמצד אחד אתם בלב העניינים ומצד שני תקבלו פרטיות יוקרתית. כל אירוח אצלנו כולל בקבוק שמפניה גדול, מוזיקה מגוונת ואווירה ייחודית.",
  },
  {
    icon: "🥂",
    title: "הפלגה פרטית וחלומית",
    text: "חברת לידר מזמינה אותכם להנות מהפלגה פרטית וחלומית. בהפלגה תצאו עם סקיפר מקצועי על אחת היאכטות שלנו ותוכלו להתפנק מהנופים, להשתזף בשמש, ללגום מהיין האיכותי, להנות מהמוזיקה שלכם ברקע.",
  },
  {
    icon: "👥",
    title: "יום כיף לארגונים וקבוצות",
    text: "מחפשים להעביר יום כיף בים עם החברים או עובדי החברה? אנחנו מזמינים אותכם ליום כיף שכולו שייט מהנה!",
  },
  {
    icon: "💬",
    title: "שירות שמתחיל כבר עכשיו",
    text: "מבחינתנו השירות מתחיל בעודכם קוראים שורות אלו, ממשיך בשיחת הטלפון שבה נשמע אתכם, נכיר אתכם, מה אתם חולמים וכיצד ניתן להגשים את רצונכם ולהתאים לכם חווית שייט אישית בדיוק עבורכם.",
  },
  {
    icon: "📍",
    title: "מרינה הרצליה",
    text: "אנו ממוקמים במרינה המפוארת בהרצליה המשמשת כפלטפורמה ייחודית ויוקרתית ונגישה לכל אירוע. היאכטות שלנו מאובזרות בכל הנדרש על מנת לדאוג לנוחות, להנאה, ולפרטיות שלכם.",
  },
];
---

<Layout
  title="יאכטות הרצליה🐬 - שייט לזוג - הפלגה בהרצליה - יום גיבוש - ⚓"
  description="לידר הפלגות יש לנו מגוון של יאכטות לכל בקשה. בין יאכטות קטנות לשייט רומנטי לזוג עד יאכטות מפוארות וקטמרנים לשייט יוקרתי מפנק דייג חוותי ללא צורך ברישיון דייג"
>
  <div class="hz-page" dir="rtl">
    <!-- ══ HERO ══ -->
    <section class="hz-hero">
      <div class="hz-hero-bg"></div>
      <div class="hz-hero-inner">
        <div class="hz-eyebrow">
          <span class="dot"></span>
          מרינה הרצליה
        </div>
        <h1>שייט בהרצליה</h1>
        <p>
          הפלגות יאכטה פרטיות, ארוחות על הים, דייג חוויתי וצימר ימי — הכל בלב
          מרינה הרצליה היוקרתית
        </p>
        <div class="hz-hero-btns">
          <a href="/צור-קשר" class="hz-btn primary">לפרטים והזמנה</a>
          <a
            href="https://wa.me/972533403449"
            target="_blank"
            rel="noopener"
            class="hz-btn wa"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="currentColor"
              width="18"
              height="18"
              ><path
                d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"
              ></path></svg
            >
            WhatsApp
          </a>
        </div>
      </div>
    </section>

    <!-- ══ SERVICES CARDS ══ -->
    <section class="hz-services">
      <div class="hz-inner">
        <div class="hz-section-header">
          <div class="hz-eyebrow-sm">מה אנחנו מציעים</div>
          <h2>חוויות שלא תשכחו</h2>
          <p>בחרו את ההפלגה המושלמת עבורכם — אנחנו נדאג לשאר</p>
        </div>

        <div class="hz-grid">
          {
            services.slice(0, 2).map((s, i) => (
              <a
                href={s.href}
                class="hz-card"
                style={`--c:${s.color}; --i:${i}`}
              >
                <div class="hz-card-img">
                  <Image
                    src={cardImg}
                    width={600}
                    height={400}
                    format="webp"
                    alt={s.alt}
                    class="hz-img"
                    loading={i < 2 ? "eager" : "lazy"}
                  />
                  <div class="hz-badge" style={`background:${s.color}`}>
                    {s.tag}
                  </div>
                </div>
                <div class="hz-card-body">
                  <div class="hz-card-icon">{s.icon}</div>
                  <h3>{s.title}</h3>
                  <p>{s.desc}</p>
                  <span class="hz-card-cta" style={`color:${s.color}`}>
                    לפרטים
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                      width="14"
                      height="14"
                    >
                      <polyline points="15 18 9 12 15 6" />
                    </svg>
                  </span>
                </div>
                <div class="hz-glow" style={`background:${s.color}`} />
              </a>
            ))
          }
        </div>

        <div class="hz-mid-text" style="margin: 3rem auto 2rem auto;">
          <p class="hmt-main">
            <strong>מרינה הרצליה</strong><br />
            אנו ממוקמים במרינה המפוארת בהרצליה המשמשת כפלטפורמה ייחודית ויוקרתית ונגישה
            לכל אירוע. היאכטות שלנו מאובזרות בכל הנדרש על מנת לדאוג לנוחות, להנאה,
            ולפרטיות שלכם.
          </p>
        </div>

        <div class="hz-maruna-out-of-frame">
          <Image
            src={marunaImg}
            width={1200}
            height={675}
            format="webp"
            alt="מבט מרחפן על מרינה הרצליה מי ים"
            class="hz-maruna-img-stretch"
            loading="lazy"
          />
        </div>

        <div class="hz-grid">
          {
            services.slice(2, 4).map((s, i) => (
              <a
                href={s.href}
                class="hz-card"
                style={`--c:${s.color}; --i:${i + 2}`}
              >
                <div class="hz-card-img">
                  <Image
                    src={cardImg}
                    width={600}
                    height={400}
                    format="webp"
                    alt={s.alt}
                    class="hz-img"
                    loading="lazy"
                  />
                  <div class="hz-badge" style={`background:${s.color}`}>
                    {s.tag}
                  </div>
                </div>
                <div class="hz-card-body">
                  <div class="hz-card-icon">{s.icon}</div>
                  <h3>{s.title}</h3>
                  <p>{s.desc}</p>
                  <span class="hz-card-cta" style={`color:${s.color}`}>
                    לפרטים
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                      width="14"
                      height="14"
                    >
                      <polyline points="15 18 9 12 15 6" />
                    </svg>
                  </span>
                </div>
                <div class="hz-glow" style={`background:${s.color}`} />
              </a>
            ))
          }
        </div>

        <div class="hz-mid-text" style="margin: 3rem auto;">
          <p class="hmt-main">
            <strong>מיקום יוקרתי ופרטי</strong><br />
            היאכטות שלנו עוגנות מול טיילת המסעדות, במעלה הרציף אליו רק מורשים יכולים
            להיכנס. כך שמצד אחד אתם בלב העניינים ומצד שני תקבלו פרטיות יוקרתית. כל
            אירוח אצלנו כולל בקבוק שמפניה גדול, מוזיקה מגוונת ואווירה ייחודית.
          </p>
        </div>

        <div class="hz-grid">
          {
            services.slice(4, 6).map((s, i) => (
              <a
                href={s.href}
                class="hz-card"
                style={`--c:${s.color}; --i:${i + 4}`}
              >
                <div class="hz-card-img">
                  <Image
                    src={cardImg}
                    width={600}
                    height={400}
                    format="webp"
                    alt={s.alt}
                    class="hz-img"
                    loading="lazy"
                  />
                  <div class="hz-badge" style={`background:${s.color}`}>
                    {s.tag}
                  </div>
                </div>
                <div class="hz-card-body">
                  <div class="hz-card-icon">{s.icon}</div>
                  <h3>{s.title}</h3>
                  <p>{s.desc}</p>
                  <span class="hz-card-cta" style={`color:${s.color}`}>
                    לפרטים
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                      width="14"
                      height="14"
                    >
                      <polyline points="15 18 9 12 15 6" />
                    </svg>
                  </span>
                </div>
                <div class="hz-glow" style={`background:${s.color}`} />
              </a>
            ))
          }
        </div>

        <div class="hz-mid-text" style="margin-top: 3rem;">
          <p class="hmt-main">
            <strong>הפלגה פרטית וחלומית</strong><br />
            חברת לידר מזמינה אותכם להנות מהפלגה פרטית וחלומית. בהפלגה תצאו עם סקיפר
            מקצועי על אחת היאכטות שלנו ותוכלו להתפנק מהנופים, להשתזף בשמש, ללגום מהיין
            האיכותי, להנות מהמוזיקה שלכם ברקע.
          </p>
        </div>
      </div>
    </section>

    <!-- ══ TEXT SECTIONS ══ -->
    <section class="hz-about">
      <div class="hz-inner">
        <div class="hz-section-header">
          <div class="hz-eyebrow-sm">על השירות שלנו</div>
          <h2>הרבה יותר מסתם הפלגה</h2>
        </div>

        <div class="hz-about-grid">
          {
            textSections.map((sec, i) => (
              <div class="hz-about-card" style={`--i:${i}`}>
                <div class="hz-about-icon">{sec.icon}</div>
                <div>
                  <h3>{sec.title}</h3>
                  <p>{sec.text}</p>
                </div>
              </div>
            ))
          }
        </div>

        <PremiumSwitch
          href="/יאכטות-בהרצליה"
          title="היאכטות שלנו בהרצליה"
          subtitle="לצפייה בצי שלנו ולבחירת היאכטה המושלמת"
        />
      </div>
    </section>

    <!-- ══ MARINA PHOTO ══ -->
    <section class="hz-marina-container">
      <div class="hz-marina-wrap">
        <Image
          src={marinaImg}
          widths={[768, 1280, 1920]}
          sizes="100vw"
          format="webp"
          alt="נוף אווירי של מרינה הרצליה — עגינות יאכטות ורחובות המרינה היוקרתיים"
          class="hz-marina-img"
          loading="lazy"
        />
      </div>
      <div class="hz-marina-info">
        <div class="hz-marina-eyebrow-dark">📍 המיקום שלנו</div>
        <h2>מרינה הרצליה</h2>
        <p>
          אנו ממוקמים בלב המרינה המפוארת — רציף E, מול שער A, צמוד לקניון ארנה
        </p>
        <a
          href="https://maps.google.com/?q=מרינה+הרצליה"
          target="_blank"
          rel="noopener noreferrer"
          class="hz-btn primary"
        >
          פתח במפה ↗
        </a>
      </div>
    </section>

    <!-- ══ CTA ══ -->
    <section class="hz-cta">
      <div class="hz-cta-bg"></div>
      <div class="hz-cta-inner">
        <h2>מוכנים לצאת לים מהרצליה?</h2>
        <p>השאירו פרטים ונחזור אליכם תוך שעה לתאם הפלגה מושלמת</p>
        <div class="hz-cta-btns">
          <a href="/צור-קשר" class="hz-btn primary large">לפרטים והזמנה</a>
          <a href="tel:0533403449" class="hz-btn outline-light large"
            >053-340-3449</a
          >
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700;800;900&display=swap");

  .hz-page {
    --navy: #08047e;
    --sea: #0f7ab0;
    --gold: #d4a017;
    --gold-l: #f0c040;
    --bg: #f2f5fb;
    --white: #ffffff;
    --text: #000000;
    --text-m: #111111;
    --text-l: #222222;
    font-family: "Rubik", "Segoe UI", Arial, sans-serif;
    color: var(--text);
    background: var(--bg);
  }

  /* ══ PREMIUM MID TEXT ══ */
  .hz-mid-text {
    background: #ffffff;
    border: 4px solid #d4a017;
    border-radius: 20px;
    padding: 2.5rem 2rem;
    max-width: 800px;
    margin: 0 auto 1.5rem auto;
    box-shadow: 0 15px 45px rgba(212, 160, 23, 0.3);
    text-align: center;
  }
  .hmt-main {
    font-size: 1.15rem;
    color: #000000;
    font-weight: 500;
    line-height: 1.8;
    margin: 0;
  }
  .hmt-main strong {
    color: #0f7ab0;
    font-size: 1.3rem;
    font-weight: 900;
    display: block;
    margin-bottom: 0.5rem;
  }

  /* ══ MARUNA OUT OF FRAME ══ */
  .hz-maruna-out-of-frame {
    margin: 0 auto 3rem auto;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }
  .hz-maruna-img-stretch {
    width: 100%;
    height: auto;
    display: block;
  }

  /* ══ HERO ══ */
  .hz-hero {
    position: relative;
    min-height: 55vh;
    display: flex;
    align-items: center;
    overflow: hidden;
  }
  .hz-hero-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      155deg,
      #040360 0%,
      #08047e 40%,
      #0a5a8f 70%,
      #072060 100%
    );
  }
  .hz-hero-bg::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        ellipse 70% 60% at 70% 30%,
        rgba(15, 122, 176, 0.3) 0%,
        transparent 55%
      ),
      radial-gradient(
        ellipse 40% 50% at 10% 80%,
        rgba(212, 160, 23, 0.1) 0%,
        transparent 50%
      );
  }

  .hz-hero-inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
    margin: 0 auto;
    padding: 7rem 2rem 5rem;
    text-align: center;
  }

  .hz-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    color: #ffffff;
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 1px;
    padding: 0.4rem 1rem;
    border-radius: 30px;
    margin-bottom: 1.5rem;
    backdrop-filter: blur(6px);
  }
  .dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #25d366;
    box-shadow: 0 0 8px #25d366;
    animation: blink 2s ease-in-out infinite;
  }
  @keyframes blink {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.3;
    }
  }

  .hz-hero-inner h1 {
    font-size: clamp(2.4rem, 6vw, 3.8rem);
    font-weight: 900;
    color: white;
    margin: 0 0 1rem;
    line-height: 1.1;
  }
  .hz-hero-inner p {
    font-size: 1.25rem;
    color: #ffffff;
    line-height: 1.7;
    max-width: 580px;
    margin: 0 auto 2rem;
  }

  .hz-hero-btns {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ══ BUTTONS ══ */
  .hz-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9rem 1.9rem;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.25s;
  }
  .hz-btn.primary {
    background: linear-gradient(135deg, var(--gold), var(--gold-l));
    color: var(--navy);
    box-shadow: 0 8px 24px rgba(212, 160, 23, 0.4);
  }
  .hz-btn.primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 14px 36px rgba(212, 160, 23, 0.55);
  }
  .hz-btn.wa {
    background: #25d366;
    color: white;
  }
  .hz-btn.wa:hover {
    background: #128c7e;
    transform: translateY(-3px);
  }
  .hz-btn.outline-light {
    border: 2px solid rgba(255, 255, 255, 0.35);
    color: white;
    background: rgba(255, 255, 255, 0.08);
  }
  .hz-btn.outline-light:hover {
    background: rgba(255, 255, 255, 0.16);
    transform: translateY(-3px);
  }
  .hz-btn.large {
    padding: 1.05rem 2.2rem;
    font-size: 1.05rem;
  }

  /* ══ SHARED ══ */
  .hz-inner {
    max-width: 1120px;
    margin: 0 auto;
    padding: 5rem 1.5rem;
  }
  .hz-section-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  .hz-eyebrow-sm {
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--sea);
    margin-bottom: 0.55rem;
  }
  .hz-section-header h2 {
    font-size: clamp(1.7rem, 3.5vw, 2.4rem);
    font-weight: 800;
    color: var(--text);
    margin: 0 0 0.8rem;
  }
  .hz-section-header p {
    font-size: 1.2rem;
    color: var(--text-l);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* ══ SERVICES GRID ══ */
  .hz-services {
    background: var(--bg);
  }

  .hz-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
  }

  .hz-card {
    position: relative;
    overflow: hidden;
    background: var(--white);
    border-radius: 20px;
    text-decoration: none;
    color: var(--text);
    box-shadow: 0 4px 20px rgba(8, 4, 126, 0.07);
    border: 1px solid rgba(8, 4, 126, 0.05);
    display: flex;
    flex-direction: column;
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
    animation: fadeUp 0.5s ease both;
    animation-delay: calc(var(--i) * 0.1s);
  }
  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .hz-card:hover {
    transform: translateY(-7px);
    box-shadow: 0 20px 50px rgba(8, 4, 126, 0.15);
  }
  .hz-card:hover .hz-glow {
    opacity: 0.06;
  }

  .hz-card-img {
    position: relative;
    width: 100%;
    aspect-ratio: 3 / 2;
    overflow: hidden;
    border-radius: 20px 20px 0 0;
    flex-shrink: 0;
  }
  .hz-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.5s ease;
  }
  .hz-card:hover .hz-img {
    transform: scale(1.06);
  }

  .hz-badge {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    font-size: 0.85rem;
    font-weight: 700;
    color: white;
    padding: 0.28rem 0.7rem;
    border-radius: 20px;
    backdrop-filter: blur(4px);
  }

  .hz-card-body {
    padding: 1.2rem 1.4rem 1.4rem;
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    flex: 1;
  }
  .hz-card-icon {
    font-size: 1.9rem;
    transition: transform 0.3s;
  }
  .hz-card:hover .hz-card-icon {
    transform: scale(1.15) rotate(-5deg);
  }
  .hz-card-body h3 {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text);
    margin: 0;
  }
  .hz-card-body p {
    font-size: 1.05rem;
    color: var(--text-l);
    line-height: 1.6;
    margin: 0;
    flex: 1;
  }

  .hz-card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.95rem;
    font-weight: 800;
    margin-top: 0.3rem;
    transition: gap 0.2s;
  }
  .hz-card:hover .hz-card-cta {
    gap: 0.55rem;
  }

  .hz-glow {
    position: absolute;
    bottom: -40px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 120px;
    border-radius: 50%;
    filter: blur(40px);
    opacity: 0;
    transition: opacity 0.35s;
    pointer-events: none;
  }

  /* ══ ABOUT SECTIONS ══ */
  .hz-about {
    background: var(--white);
  }

  .hz-about-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .hz-about-card {
    display: flex;
    gap: 1.1rem;
    background: var(--bg);
    border-radius: 18px;
    padding: 1.5rem;
    border-right: 4px solid var(--sea);
    animation: fadeUp 0.5s ease both;
    animation-delay: calc(var(--i) * 0.08s);
    transition:
      transform 0.25s,
      box-shadow 0.25s;
  }
  .hz-about-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(8, 4, 126, 0.1);
  }

  .hz-about-icon {
    font-size: 2rem;
    flex-shrink: 0;
    width: 3rem;
    text-align: center;
    margin-top: 0.1rem;
  }
  .hz-about-card h3 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--navy);
    margin: 0 0 0.5rem;
  }
  .hz-about-card p {
    font-size: 1.05rem;
    color: var(--text-m);
    line-height: 1.75;
    margin: 0;
  }

  /* ══ CTA ══ */
  .hz-cta {
    position: relative;
    overflow: hidden;
    background: linear-gradient(155deg, #040360 0%, #08047e 45%, #0a5a8f 100%);
  }
  .hz-cta-bg {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse 60% 60% at 80% 50%,
      rgba(15, 122, 176, 0.25) 0%,
      transparent 55%
    );
  }
  .hz-cta-inner {
    position: relative;
    z-index: 1;
    max-width: 720px;
    margin: 0 auto;
    padding: 5rem 2rem;
    text-align: center;
  }
  .hz-cta-inner h2 {
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    font-weight: 800;
    color: white;
    margin: 0 0 0.75rem;
  }
  .hz-cta-inner p {
    font-size: 1.25rem;
    color: #ffffff;
    margin: 0 0 2rem;
    line-height: 1.6;
  }
  .hz-cta-btns {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ══ RESPONSIVE ══ */
  @media (max-width: 768px) {
    .hz-maruna-out-of-frame {
      border-radius: 0;
      width: calc(100% + 2.5rem);
      margin-left: -1.25rem;
      margin-right: -1.25rem;
    }
    .hz-grid {
      grid-template-columns: 1fr;
    }
    .hz-about-grid {
      grid-template-columns: 1fr;
    }
    .hz-hero-inner {
      padding: 6rem 1.5rem 4rem;
    }
    .hz-inner {
      padding: 3.5rem 1.25rem;
    }
    .hz-btn.large {
      width: 100%;
      max-width: 320px;
      justify-content: center;
    }
    .hz-cta-btns {
      flex-direction: column;
      align-items: center;
    }
  }

  /* ══ MARINA PHOTO ══ */
  .hz-marina-container {
    width: 100%;
    background: var(--white);
    padding-bottom: 4rem;
  }
  .hz-marina-wrap {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 6;
    overflow: hidden;
  }
  .hz-marina-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 60%;
    display: block;
    transition: transform 6s ease;
  }
  .hz-marina-container:hover .hz-marina-img {
    transform: scale(1.03);
  }

  /* Info Below Photo */
  .hz-marina-info {
    max-width: 800px;
    width: 92%;
    margin: 0 auto;
    transform: translateY(-3.5rem);
    position: relative;
    z-index: 10;
    background: #ffffff;
    border-radius: 20px;
    padding: 3rem 2rem;
    text-align: center;
    box-shadow: 0 15px 45px rgba(212, 160, 23, 0.3);
    border: 4px solid #d4a017;
    display: flex;
    flex-direction: column;
    align-items: center !important;
    justify-content: center;
  }
  .hz-marina-eyebrow-dark {
    font-size: 0.95rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--sea);
    margin: 0 auto 0.5rem auto !important;
    text-align: center;
  }
  .hz-marina-info h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.6rem);
    font-weight: 800;
    color: var(--text);
    margin: 0 auto 0.85rem auto !important;
    line-height: 1.15;
    text-align: center;
  }
  .hz-marina-info p {
    font-size: 1.15rem;
    color: var(--text-l);
    margin: 0 auto 1.5rem auto !important;
    line-height: 1.65;
    max-width: 500px;
    text-align: center;
  }

  @media (max-width: 768px) {
    .hz-marina-wrap {
      aspect-ratio: 16 / 9;
    }
    .hz-marina-info {
      width: 92%;
      transform: translateY(-1.5rem);
      padding: 1.25rem 1rem;
      border-radius: 16px;
      margin: 0 auto;
    }
    .hz-marina-eyebrow-dark {
      font-size: 0.8rem;
      margin: 0 auto 0.3rem auto !important;
    }
    .hz-marina-info h2 {
      font-size: 1.5rem;
      margin: 0 auto 0.5rem auto !important;
    }
    .hz-marina-info p {
      font-size: 0.95rem;
      line-height: 1.45;
      margin: 0 auto 1rem auto !important;
    }
  }
</style>
