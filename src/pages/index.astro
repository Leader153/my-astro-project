---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import cardImg from '../assets/Romantic cruise 2.webp';

const services = [
  {
    icon: '💑',
    title: 'שייט רומנטי',
    desc: 'רגעים קסומים לשניים. שקיעה, כוכבים ורוח ים — החל מ-550 ₪',
    tag: 'לזוגות',
    color: '#e91e8c',
    href: '/צור-קשר',
    alt: 'זוג מאושר על יאכטה בשקיעה רומנטית בים התיכון',
  },
  {
    icon: '🎉',
    title: 'מסיבת רווקות / רווקים',
    desc: 'מסיבה בלתי נשכחת על הסיפון עם חברים. אלכוהול, מוסיקה והרבה כיף',
    tag: 'פופולרי',
    color: '#7c3aed',
    href: '/צור-קשר',
    alt: 'קבוצת חברים חוגגת מסיבת רווקות על יאכטה פרטית',
  },
  {
    icon: '🎂',
    title: 'יום הולדת על יאכטה',
    desc: 'תחגגו יום הולדת שלא תשכחו לעולם — ביאכטה פרטית עם כל החבר\'ים',
    tag: 'מומלץ',
    color: '#d4a017',
    href: '/צור-קשר',
    alt: 'חגיגת יום הולדת על סיפון יאכטה עם חברים ועוגה',
  },
  {
    icon: '👔',
    title: 'גיבוש ואירועי חברה',
    desc: 'ימי גיבוש לעובדים ואירוח עסקי ביאכטה — חוויה שתחזק את הצוות',
    tag: 'עסקי',
    color: '#0f7ab0',
    href: '/צור-קשר',
    alt: 'צוות עובדים ביום גיבוש על יאכטה עם נוף ים',
  },
  {
    icon: '👨‍👩‍👧‍👦',
    title: 'הפלגה משפחתית',
    desc: 'יום בים עם כל המשפחה — בטוח, מהנה ובלתי נשכח לכל הגילאים',
    tag: 'משפחות',
    color: '#059669',
    href: '/צור-קשר',
    alt: 'משפחה שמחה עם ילדים נהנים מהפלגה ביאכטה בים',
  },
  {
    icon: '💍',
    title: 'הצעת נישואין',
    desc: 'הפתיעו את האהוב/ה שלכם ביאכטה — רגע שתזכרו לכל החיים',
    tag: 'מיוחד',
    color: '#e91e8c',
    href: '/צור-קשר',
    alt: 'זוג לבוש בבגדי חתונה שניים על יאכטה מפרשית — הצעת נישואין רומנטית',
  },
];

const features = [
  { icon: '⭐', title: '10+ שנות ניסיון', desc: 'מאז 2011, מאות הפלגות מאושרות' },
  { icon: '⛵', title: 'צי של 4 יאכטות', desc: 'יאכטות ברמת פאר גבוהה לכל תקציב' },
  { icon: '👨‍✈️', title: 'סקיפרים מוסמכים', desc: 'מקצועיים, אוהבי ים ושירותיים' },
  { icon: '🛡️', title: 'בטיחות מקסימלית', desc: 'מבוטחים, מאובזרים וזמינים 24/7' },
  { icon: '📍', title: 'חיפה + הרצליה', desc: 'שתי מרינות — כנסו בנוח אליכם' },
  { icon: '💰', title: 'מחיר הוגן', desc: 'איכות יוקרה במחיר שמתאים לכולם' },
];

const testimonials = [
  {
    name: 'שירה כ.',
    event: 'מסיבת רווקות',
    stars: 5,
    text: 'חוויה מטורפת! הסקיפר היה מדהים, היאכטה יפהפייה והארגון ברמה אחרת. כל החברות עוד מדברות על זה!',
    avatar: 'ש',
  },
  {
    name: 'דניאל מ.',
    event: 'יום הולדת',
    stars: 5,
    text: 'הזמנו ליום הולדת ה-40 שלי — פשוט wow. שירות אישי, תשומת לב לפרטים, ורגעים שלא אשכח.',
    avatar: 'ד',
  },
  {
    name: 'איתי ו.',
    event: 'גיבוש חברה',
    stars: 5,
    text: 'גיבוש חברה מושלם! הצוות היה מקצועי ואירח אותנו ברמה גבוהה מאוד. ממליץ בחום לכל חברה.',
    avatar: 'א',
  },
];
---

<Layout title="Leader הפלגות חיפה — שייט יוקרתי ביאכטה">
<div class="home-page" dir="rtl">

  <!-- ══════════════════════════════════════
       HERO — Full Screen
  ══════════════════════════════════════ -->
  <section class="hero" aria-label="כותרת ראשית">
    <!-- Video/gradient bg -->
    <div class="hero-bg">
      <div class="hero-gradient"></div>
      <div class="hero-waves-anim" aria-hidden="true">
        <div class="wave-anim w1"></div>
        <div class="wave-anim w2"></div>
        <div class="wave-anim w3"></div>
      </div>
    </div>

    <div class="hero-inner">
      <div class="hero-badge">
        <span class="badge-dot"></span>
        חיפה · הרצליה · 24/7
      </div>

      <h1>
        <span class="h1-line1">הפלגות יוקרתיות</span>
        <span class="h1-line2">ביאכטה פרטית</span>
        <span class="h1-accent">בלתי נשכחות ⚓</span>
      </h1>

      <p class="hero-desc">
        שייט רומנטי, מסיבות, ימי גיבוש ואירועי חברה — אנחנו נהפוך כל הפלגה לחוויה שתספרו עליה לנצח
      </p>

      <div class="hero-actions">
        <a href="/צור-קשר" class="hero-btn primary">
          קבלו הצעת מחיר
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="18" height="18"><polyline points="15 18 9 12 15 6"/></svg>
        </a>
        <a href="tel:0533403449" class="hero-btn outline">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="18" height="18"><path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/></svg>
          053-340-3449
        </a>
      </div>

      <!-- Trust chips -->
      <div class="hero-trust">
        <span class="trust-chip">✅ מבוטח צד ג'</span>
        <span class="trust-chip">⭐ 100+ ביקורות 5 כוכבים</span>
        <span class="trust-chip">🚤 4 יאכטות</span>
      </div>
    </div>

    <!-- Scroll hint -->
    <div class="scroll-hint" aria-hidden="true">
      <div class="scroll-line"></div>
      <span>גלול למטה</span>
    </div>
  </section>

  <!-- ══════════════════════════════════════
       SERVICES — Card Grid
  ══════════════════════════════════════ -->
  <section class="services-section">
    <div class="section-inner">
      <div class="section-header">
        <div class="eyebrow">בחרו את ההפלגה שלכם</div>
        <h2>לכל אירוע — הפלגה מושלמת</h2>
        <p>מגוון חבילות לכל מטרה — מזוגות שמחפשים רומנטיקה ועד קבוצות גדולות לאירוע בלתי נשכח</p>
      </div>

      <div class="services-grid">
        {services.map((s, i) => (
          <a href={s.href} class="service-card" style={`--c:${s.color}; --i:${i}`}>
            <!-- Card image -->
            <div class="sc-img-wrap">
              <Image
                src={cardImg}
                width={600}
                height={400}
                format="webp"
                alt={s.alt}
                class="sc-img"
                loading={i < 3 ? 'eager' : 'lazy'}
              />
              <div class="sc-tag" style={`background:${s.color}; color:#fff`}>{s.tag}</div>
            </div>
            <!-- Card body -->
            <div class="sc-body">
              <div class="sc-icon">{s.icon}</div>
              <h3>{s.title}</h3>
              <p>{s.desc}</p>
              <div class="sc-arrow" style={`color:${s.color}`}>
                קבלו פרטים
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="15" height="15"><polyline points="15 18 9 12 15 6"/></svg>
              </div>
            </div>
            <div class="sc-glow" style={`background:${s.color}`}></div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════
       WHY US — Feature Strip
  ══════════════════════════════════════ -->
  <section class="why-section">
    <div class="why-bg" aria-hidden="true"></div>
    <div class="section-inner">
      <div class="section-header light">
        <div class="eyebrow light">למה לבחור בנו</div>
        <h2>Leader הפלגות — החוויה הימית הטובה ביותר</h2>
      </div>
      <div class="features-grid">
        {features.map((f, i) => (
          <div class="feature-card" style={`--i:${i}`}>
            <div class="fc-icon">{f.icon}</div>
            <h3>{f.title}</h3>
            <p>{f.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════
       LOCATIONS — 2-col split
  ══════════════════════════════════════ -->
  <section class="locations-section">
    <div class="section-inner">
      <div class="section-header">
        <div class="eyebrow">אנחנו נמצאים ב</div>
        <h2>שתי מרינות — קרוב אליכם</h2>
      </div>
      <div class="locations-split">
        <div class="loc-card haifa">
          <div class="loc-emoji">⚓</div>
          <h3>חיפה</h3>
          <p>פארק קישון – מעגנת שביט<br/>רחוב הדייגים 3</p>
          <a href="https://maps.google.com/?q=פארק+קישון+מעגנת+שביט+חיפה" target="_blank" rel="noopener" class="loc-map-btn">פתח במפה ↗</a>
        </div>
        <div class="loc-divider" aria-hidden="true">
          <span>+</span>
        </div>
        <div class="loc-card herzliya">
          <div class="loc-emoji">🌊</div>
          <h3>הרצליה</h3>
          <p>מרינה הרצליה<br/>רציף E מול שער A — קניון ארנה</p>
          <a href="https://maps.google.com/?q=מרינה+הרצליה" target="_blank" rel="noopener" class="loc-map-btn">פתח במפה ↗</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════
       TESTIMONIALS
  ══════════════════════════════════════ -->
  <section class="testimonials-section">
    <div class="section-inner">
      <div class="section-header">
        <div class="eyebrow">מה אומרים עלינו</div>
        <h2>לקוחות מרוצים מספרים</h2>
      </div>
      <div class="testimonials-grid">
        {testimonials.map((t, i) => (
          <div class="testimonial-card" style={`--i:${i}`}>
            <div class="tc-stars">
              {'★'.repeat(t.stars)}
            </div>
            <p class="tc-text">"{t.text}"</p>
            <div class="tc-author">
              <div class="tc-avatar">{t.avatar}</div>
              <div>
                <div class="tc-name">{t.name}</div>
                <div class="tc-event">{t.event}</div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════
       FINAL CTA BAND
  ══════════════════════════════════════ -->
  <section class="cta-band">
    <div class="cta-band-bg" aria-hidden="true">
      <div class="cta-circle c1"></div>
      <div class="cta-circle c2"></div>
    </div>
    <div class="cta-band-inner">
      <div class="cta-text">
        <h2>מוכנים לצאת לים?</h2>
        <p>השאירו פרטים ונחזור אליכם תוך שעה לתאם הפלגה מושלמת</p>
      </div>
      <div class="cta-buttons">
        <a href="/צור-קשר" class="cta-main-btn">
          לפרטים והזמנה
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="18" height="18"><polyline points="15 18 9 12 15 6"/></svg>
        </a>
        <a href="https://wa.me/972533403449" target="_blank" rel="noopener" class="cta-wa-btn">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="18" height="18"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
          WhatsApp עכשיו
        </a>
      </div>
    </div>
  </section>

</div>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700;800;900&display=swap');

  /* ════════════ VARS ════════════ */
  .home-page {
    --navy:   #08047e;
    --navy-d: #05036a;
    --sea:    #0f7ab0;
    --gold:   #d4a017;
    --gold-l: #f0c040;
    --bg:     #f2f5fb;
    --white:  #ffffff;
    --text:   #1a1a2e;
    --text-m: #3a3a5c;
    --text-l: #6b7280;

    font-family: 'Rubik', 'Segoe UI', Arial, sans-serif;
    color: var(--text);
    background: var(--bg);
  }

  /* ════════════ HERO ════════════ */
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex; align-items: center;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute; inset: 0; z-index: 0;
  }

  .hero-gradient {
    position: absolute; inset: 0;
    background:
      radial-gradient(ellipse 80% 70% at 60% 20%, rgba(15,122,176,0.35) 0%, transparent 55%),
      radial-gradient(ellipse 60% 50% at 20% 80%, rgba(212,160,23,0.12) 0%, transparent 50%),
      linear-gradient(160deg, #040360 0%, #08047e 35%, #0a5a8f 65%, #06386e 100%);
  }

  /* Animated shimmer waves */
  .hero-waves-anim {
    position: absolute; inset: 0; overflow: hidden;
  }
  .wave-anim {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.06);
    animation: expand 8s ease-in-out infinite;
  }
  .w1 { width: 600px; height: 600px; top: -200px; right: -100px; animation-delay: 0s; }
  .w2 { width: 400px; height: 400px; bottom: -100px; left: -50px; animation-delay: 3s; }
  .w3 { width: 300px; height: 300px; top: 30%; left: 40%; animation-delay: 6s; background: rgba(212,160,23,0.03); border-color: rgba(212,160,23,0.08); }
  @keyframes expand {
    0%,100% { transform: scale(1); opacity: 0.6; }
    50%      { transform: scale(1.1); opacity: 0.2; }
  }

  .hero-inner {
    position: relative; z-index: 1;
    max-width: 820px;
    margin: 0 auto;
    padding: 7rem 2rem 5rem;
    text-align: center;
  }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 0.6rem;
    background: rgba(255,255,255,0.1);
    border: 1px solid rgba(255,255,255,0.2);
    color: rgba(255,255,255,0.8);
    font-size: 0.8rem; font-weight: 600; letter-spacing: 1px;
    padding: 0.45rem 1.1rem; border-radius: 30px;
    margin-bottom: 1.75rem;
    backdrop-filter: blur(8px);
  }
  .badge-dot {
    width: 7px; height: 7px; border-radius: 50%;
    background: #25D366;
    box-shadow: 0 0 8px #25D366;
    animation: blink 2s ease-in-out infinite;
  }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }

  .hero-inner h1 {
    display: flex; flex-direction: column;
    font-size: clamp(2.6rem, 7vw, 4.4rem);
    font-weight: 900; line-height: 1.1;
    margin: 0 0 1.25rem;
  }
  .h1-line1 { color: rgba(255,255,255,0.88); }
  .h1-line2 { color: white; }
  .h1-accent {
    font-size: 0.6em;
    color: var(--gold-l);
    font-weight: 700;
    margin-top: 0.15em;
    letter-spacing: 1px;
  }

  .hero-desc {
    font-size: 1.1rem; color: rgba(255,255,255,0.74);
    line-height: 1.7; max-width: 560px;
    margin: 0 auto 2.25rem;
  }

  .hero-actions {
    display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;
    margin-bottom: 1.75rem;
  }

  .hero-btn {
    display: inline-flex; align-items: center; gap: 0.55rem;
    padding: 0.95rem 2rem; border-radius: 14px;
    font-size: 1.05rem; font-weight: 700;
    text-decoration: none; transition: all 0.25s ease;
  }
  .hero-btn.primary {
    background: linear-gradient(135deg, var(--gold), var(--gold-l));
    color: var(--navy);
    box-shadow: 0 8px 28px rgba(212,160,23,0.45);
  }
  .hero-btn.primary:hover { transform: translateY(-3px); box-shadow: 0 14px 36px rgba(212,160,23,0.6); }
  .hero-btn.outline {
    border: 2px solid rgba(255,255,255,0.35);
    color: white; background: rgba(255,255,255,0.08);
    backdrop-filter: blur(6px);
  }
  .hero-btn.outline:hover { background: rgba(255,255,255,0.16); transform: translateY(-3px); }

  .hero-trust {
    display: flex; gap: 0.7rem; justify-content: center; flex-wrap: wrap;
  }
  .trust-chip {
    font-size: 0.78rem; font-weight: 600;
    color: rgba(255,255,255,0.65);
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.12);
    padding: 0.3rem 0.8rem; border-radius: 20px;
  }

  /* scroll hint */
  .scroll-hint {
    position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: 0.4rem;
    color: rgba(255,255,255,0.35); font-size: 0.72rem; letter-spacing: 1px;
    animation: float-down 2.5s ease-in-out infinite;
  }
  .scroll-line {
    width: 1px; height: 40px;
    background: linear-gradient(to bottom, transparent, rgba(255,255,255,0.4));
  }
  @keyframes float-down { 0%,100%{transform:translateX(-50%) translateY(0)} 50%{transform:translateX(-50%) translateY(8px)} }

  /* ════════════ SHARED SECTION ════════════ */
  .section-inner {
    max-width: 1120px; margin: 0 auto;
    padding: 5rem 1.5rem;
  }

  .section-header { text-align: center; margin-bottom: 3rem; }
  .section-header.light h2 { color: white; }

  .eyebrow {
    font-size: 0.72rem; font-weight: 700; letter-spacing: 2.5px;
    text-transform: uppercase; color: var(--sea);
    margin-bottom: 0.6rem;
  }
  .eyebrow.light { color: rgba(255,255,255,0.55); }

  .section-header h2 {
    font-size: clamp(1.7rem, 3.5vw, 2.4rem); font-weight: 800;
    color: var(--text); margin: 0 0 0.85rem;
  }
  .section-header p {
    font-size: 1rem; color: var(--text-l);
    max-width: 580px; margin: 0 auto; line-height: 1.7;
  }

  /* ════════════ SERVICES ════════════ */
  .services-section { background: var(--bg); }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .service-card {
    position: relative; overflow: hidden;
    background: var(--white);
    border-radius: 20px;
    padding: 0;                        /* убираем общий padding — у каждой части свой */
    text-decoration: none; color: var(--text);
    box-shadow: 0 4px 20px rgba(8,4,126,0.07);
    border: 1px solid rgba(8,4,126,0.05);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    animation: fadeUp 0.5s ease both;
    animation-delay: calc(var(--i) * 0.08s);
    display: flex; flex-direction: column;
  }
  @keyframes fadeUp {
    from { opacity:0; transform:translateY(24px); }
    to   { opacity:1; transform:translateY(0); }
  }
  .service-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 50px rgba(8,4,126,0.16);
  }
  .service-card:hover .sc-glow { opacity: 0.06; }

  /* ── Image wrap ── */
  .sc-img-wrap {
    position: relative;
    width: 100%;
    aspect-ratio: 3 / 2;     /* фиксированная пропорция */
    overflow: hidden;
    border-radius: 20px 20px 0 0;
    flex-shrink: 0;
  }
  .sc-img {
    width: 100%; height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.5s ease;
  }
  .service-card:hover .sc-img { transform: scale(1.06); }

  /* ── Tag badge overlaid on image (top-right for RTL) ── */
  .sc-tag {
    position: absolute;
    top: 0.75rem; right: 0.75rem;
    font-size: 0.68rem; font-weight: 700; letter-spacing: 0.5px;
    padding: 0.28rem 0.7rem; border-radius: 20px;
    backdrop-filter: blur(4px);
  }

  /* ── Card body ── */
  .sc-body {
    padding: 1.2rem 1.4rem 1.35rem;
    display: flex; flex-direction: column; gap: 0.45rem;
    flex: 1;
  }

  .sc-icon { font-size: 2rem; transition: transform 0.3s; }
  .service-card:hover .sc-icon { transform: scale(1.15) rotate(-5deg); }

  .sc-body h3 { font-size: 1.05rem; font-weight: 700; color: var(--text); margin: 0; }
  .sc-body p  { font-size: 0.875rem; color: var(--text-l); line-height: 1.6; margin: 0; flex: 1; }

  .sc-arrow {
    display: inline-flex; align-items: center; gap: 0.35rem;
    font-size: 0.82rem; font-weight: 700;
    margin-top: auto;
    transition: gap 0.2s;
  }
  .service-card:hover .sc-arrow { gap: 0.55rem; }

  .sc-glow {
    position: absolute; bottom: -40px; left: 50%; transform: translateX(-50%);
    width: 120px; height: 120px; border-radius: 50%;
    filter: blur(40px); opacity: 0;
    transition: opacity 0.35s ease;
    pointer-events: none;
  }

  /* ════════════ WHY US ════════════ */
  .why-section {
    position: relative;
    overflow: hidden;
  }
  .why-bg {
    position: absolute; inset: 0;
    background: linear-gradient(155deg, var(--navy-d) 0%, var(--navy) 45%, #0a5a8f 100%);
  }
  .why-section .section-inner { position: relative; z-index: 1; }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .feature-card {
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 18px;
    padding: 1.75rem 1.4rem;
    text-align: center;
    backdrop-filter: blur(8px);
    transition: background 0.25s, transform 0.25s;
    animation: fadeUp 0.5s ease both;
    animation-delay: calc(var(--i) * 0.08s);
  }
  .feature-card:hover { background: rgba(255,255,255,0.12); transform: translateY(-5px); }

  .fc-icon { font-size: 2.2rem; margin-bottom: 0.75rem; }
  .feature-card h3 { font-size: 1rem; font-weight: 700; color: white; margin: 0 0 0.45rem; }
  .feature-card p  { font-size: 0.85rem; color: rgba(255,255,255,0.62); line-height: 1.55; margin: 0; }

  /* ════════════ LOCATIONS ════════════ */
  .locations-section { background: var(--white); }

  .locations-split {
    display: flex; align-items: stretch; gap: 0;
    background: var(--bg); border-radius: 22px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(8,4,126,0.1);
  }

  .loc-card {
    flex: 1; padding: 3rem 2.5rem;
    display: flex; flex-direction: column; gap: 0.75rem;
  }
  .loc-card.haifa { background: linear-gradient(135deg, var(--navy) 0%, #0a5080 100%); color: white; }
  .loc-card.herzliya { background: var(--white); color: var(--text); }

  .loc-emoji { font-size: 2.8rem; }
  .loc-card h3 { font-size: 1.5rem; font-weight: 800; margin: 0; }
  .loc-card.haifa h3 { color: white; }
  .loc-card p { font-size: 0.95rem; line-height: 1.6; margin: 0; opacity: 0.8; flex: 1; }

  .loc-map-btn {
    display: inline-flex; align-items: center;
    font-weight: 700; font-size: 0.88rem;
    text-decoration: none; margin-top: auto;
    padding: 0.6rem 1.1rem; border-radius: 10px;
    transition: all 0.2s; align-self: flex-start;
  }
  .haifa .loc-map-btn {
    background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.25);
  }
  .haifa .loc-map-btn:hover { background: rgba(255,255,255,0.25); }
  .herzliya .loc-map-btn { background: var(--navy); color: white; }
  .herzliya .loc-map-btn:hover { background: var(--sea); }

  .loc-divider {
    width: 60px; flex-shrink: 0;
    background: var(--bg);
    display: flex; align-items: center; justify-content: center;
    font-size: 1.5rem; color: var(--text-l); font-weight: 700;
  }

  /* ════════════ TESTIMONIALS ════════════ */
  .testimonials-section { background: var(--bg); }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .testimonial-card {
    background: var(--white);
    border-radius: 18px;
    padding: 1.75rem;
    box-shadow: 0 4px 20px rgba(8,4,126,0.07);
    display: flex; flex-direction: column; gap: 1rem;
    border-top: 3px solid var(--gold);
    animation: fadeUp 0.5s ease both;
    animation-delay: calc(var(--i) * 0.1s);
    transition: transform 0.25s, box-shadow 0.25s;
  }
  .testimonial-card:hover { transform: translateY(-5px); box-shadow: 0 16px 44px rgba(8,4,126,0.14); }

  .tc-stars { color: var(--gold); font-size: 1.1rem; letter-spacing: 2px; }
  .tc-text { font-size: 0.92rem; color: var(--text-m); line-height: 1.75; margin: 0; flex: 1; font-style: italic; }
  .tc-author { display: flex; align-items: center; gap: 0.75rem; margin-top: auto; }
  .tc-avatar {
    width: 42px; height: 42px; border-radius: 50%;
    background: linear-gradient(135deg, var(--navy), var(--sea));
    color: white; font-weight: 700; font-size: 1.1rem;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .tc-name  { font-weight: 700; font-size: 0.92rem; color: var(--text); }
  .tc-event { font-size: 0.78rem; color: var(--text-l); }

  /* ════════════ CTA BAND ════════════ */
  .cta-band {
    position: relative; overflow: hidden;
    background: linear-gradient(135deg, var(--navy-d) 0%, var(--navy) 50%, #0a5a8f 100%);
  }
  .cta-band-bg { position: absolute; inset: 0; }
  .cta-circle {
    position: absolute; border-radius: 50%;
    background: rgba(255,255,255,0.04);
  }
  .c1 { width: 500px; height: 500px; top: -200px; right: -100px; }
  .c2 { width: 300px; height: 300px; bottom: -100px; left: 5%; border: 1px solid rgba(255,255,255,0.06); background: transparent; }

  .cta-band-inner {
    position: relative; z-index: 1;
    max-width: 1120px; margin: 0 auto;
    padding: 4.5rem 2rem;
    display: flex; align-items: center; justify-content: space-between;
    gap: 2rem; flex-wrap: wrap;
  }

  .cta-text { flex: 1; min-width: 240px; }
  .cta-text h2 { font-size: clamp(1.8rem, 4vw, 2.6rem); font-weight: 800; color: white; margin: 0 0 0.6rem; }
  .cta-text p  { font-size: 1rem; color: rgba(255,255,255,0.72); margin: 0; line-height: 1.6; }

  .cta-buttons { display: flex; gap: 1rem; flex-wrap: wrap; }

  .cta-main-btn {
    display: inline-flex; align-items: center; gap: 0.55rem;
    background: linear-gradient(135deg, var(--gold), var(--gold-l));
    color: var(--navy); font-weight: 800; font-size: 1.05rem;
    padding: 1rem 2.25rem; border-radius: 14px;
    text-decoration: none; transition: all 0.25s;
    box-shadow: 0 8px 28px rgba(212,160,23,0.4);
  }
  .cta-main-btn:hover { transform: translateY(-3px); box-shadow: 0 14px 40px rgba(212,160,23,0.6); }

  .cta-wa-btn {
    display: inline-flex; align-items: center; gap: 0.55rem;
    background: #25D366; color: white; font-weight: 700; font-size: 1rem;
    padding: 1rem 1.75rem; border-radius: 14px;
    text-decoration: none; transition: all 0.25s;
  }
  .cta-wa-btn:hover { background: #128C7E; transform: translateY(-3px); }

  /* ════════════ RESPONSIVE ════════════ */
  @media (max-width: 900px) {
    .services-grid { grid-template-columns: repeat(2, 1fr); }
    .features-grid { grid-template-columns: repeat(2, 1fr); }
    .testimonials-grid { grid-template-columns: 1fr; }
    .locations-split { flex-direction: column; }
    .loc-divider { width: auto; height: 50px; }
    .cta-band-inner { flex-direction: column; text-align: center; }
    .cta-buttons { justify-content: center; }
  }

  @media (max-width: 600px) {
    .services-grid { grid-template-columns: 1fr; }
    .features-grid { grid-template-columns: 1fr 1fr; }
    .hero-inner { padding: 6rem 1.25rem 4rem; }
    .section-inner { padding: 3.5rem 1.25rem; }
    .cta-main-btn, .cta-wa-btn { width: 100%; justify-content: center; }
    .hero-btn { width: 100%; max-width: 300px; justify-content: center; }
  }
</style>
